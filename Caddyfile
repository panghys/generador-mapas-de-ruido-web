:80 {
    # Redirige las peticiones de la API al backend (si tu backend usa el puerto 5000 o 3000)
    @api path /api/*
    handle @api {
        reverse_proxy backend:4009
    }

    # Redirige todo lo demás a tu Frontend
    handle {
        reverse_proxy frontend:5173
    }
}